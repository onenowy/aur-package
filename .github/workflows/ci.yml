name: Arch Package CI/CD

on:
  schedule:
    # Run daily at 00:00 UTC
    - cron: '0 0 * * *'
  workflow_dispatch:
    inputs:
      check_for_updates:
        description: 'Check upstream updates? (Uncheck to build ALL packages as-is)'
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  check-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Dependencies
        run: sudo apt-get install -y python3-pip jq && pip install 'nvchecker[rpmrepo,aur,github]'

      - name: Check for Updates
        id: check
        run: |
          chmod +x ./update-version.sh
          
          if [[ "${{ github.event_name }}" == "schedule" || "${{ inputs.check_for_updates }}" == "true" ]]; then
            ./update-version.sh
          else
            echo "Manual build mode: Building ALL packages found."
            ALL_PKGS=$(find . -maxdepth 2 -name PKGBUILD -printf '%h\n' | sed 's|./||' | jq -R . | jq -s -c .)
            
            echo "Detected packages: $ALL_PKGS"
            echo "BUILD_REQUIRED=true" >> "$GITHUB_OUTPUT"
            echo "BUILD_PACKAGES=$ALL_PKGS" >> "$GITHUB_OUTPUT"
          fi

      - name: Make Scripts Executable
        if: steps.check.outputs.BUILD_REQUIRED == 'true'
        run: chmod +x ./build_script.sh

      - name: Build Packages
        if: steps.check.outputs.BUILD_REQUIRED == 'true'
        uses: docker://archlinux:base-devel
        env:
          PACKAGES: ${{ steps.check.outputs.BUILD_PACKAGES }}
        with:
          args: /bin/bash /github/workspace/build_script.sh

      - if: steps.check.outputs.BUILD_REQUIRED == 'true'
        run: sudo chown -R $USER:$USER .

      - name: Commit Changes
        if: steps.check.outputs.BUILD_REQUIRED == 'true'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "update: packages updated"
          file_pattern: '*/PKGBUILD */.SRCINFO'

      # [Modified] Download existing DB
      - name: Fetch existing repository database
        if: steps.check.outputs.BUILD_REQUIRED == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p release_dist
          echo "Fetching existing database..."
          gh release download x86_64 -p "shelter-arch-aur.db.tar.gz" -D release_dist || true
          gh release download x86_64 -p "shelter-arch-aur.files.tar.gz" -D release_dist || true

      # [NEW] Clean up Orphaned Packages (Deleted from local)
      - name: Clean up Orphaned Packages
        if: steps.check.outputs.BUILD_REQUIRED == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Checking for orphaned packages..."
          
          # 1. Get list of valid local package directories
          find . -maxdepth 2 -name PKGBUILD -printf '%h\n' | sed 's|./||' | sort > local_pkgs.txt
          
          # 2. Get list of remote assets from GitHub Release
          # If release doesn't exist yet, ignore error
          gh release view x86_64 --json assets -q '.assets[].name' | grep '\.pkg\.tar\.zst$' | sort > remote_assets.txt || touch remote_assets.txt
          
          # 3. Identify orphans
          touch pkgs_to_remove.txt
          
          while read -r asset; do
            # Extract pkgname from filename (heuristic: name-ver-rel-arch.pkg.tar.zst)
            # Remove extension
            basename=${asset%.pkg.tar.zst}
            # Remove last 3 parts (arch, rel, ver) separated by hyphens
            # e.g. octopi-0.18.1-1-x86_64 -> octopi
            pkgname=$(echo "$basename" | rev | cut -d- -f4- | rev)
            
            # Check if this pkgname exists in local_pkgs.txt
            if ! grep -Fxq "$pkgname" local_pkgs.txt; then
              echo ":: Orphan detected: $pkgname (File: $asset)"
              
              # Delete asset from GitHub immediately
              gh release delete-asset x86_64 "$asset" -y
              
              # Add pkgname to removal list for DB update
              echo "$pkgname" >> pkgs_to_remove.txt
            fi
          done < remote_assets.txt
          
          # Move removal list to release_dist so Docker can see it
          if [ -f pkgs_to_remove.txt ]; then
             mv pkgs_to_remove.txt release_dist/
          fi

      # [Modified] Create/Update Repository Database (Handle removals)
      - name: Create Repository Database
        if: steps.check.outputs.BUILD_REQUIRED == 'true'
        run: |
          docker run --rm -v $PWD/release_dist:/repo archlinux:base-devel /bin/bash -c "
            cd /repo
            
            # 1. Remove orphaned packages from DB if list exists
            if [ -f pkgs_to_remove.txt ]; then
              echo 'Removing orphaned packages from DB...'
              # Read file into array and remove duplicates
              mapfile -t to_remove < <(sort -u pkgs_to_remove.txt)
              if [ \${#to_remove[@]} -gt 0 ]; then
                 # Run repo-remove (ignore error if pkg not in DB)
                 repo-remove shelter-arch-aur.db.tar.gz \"\${to_remove[@]}\" || true
              fi
              rm pkgs_to_remove.txt
            fi
            
            # 2. Add new packages to DB
            shopt -s nullglob
            files=(*.pkg.tar.zst)
            if [ \${#files[@]} -gt 0 ]; then
              echo 'Adding new packages to DB...'
              repo-add -n shelter-arch-aur.db.tar.gz \"\${files[@]}\"
            fi
          "

      # [Maintained] Remove Old Versions of Updated Packages
      - name: Remove Old Versions
        if: steps.check.outputs.BUILD_REQUIRED == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f release_dist/updated_pkgnames.txt ]; then
            echo "Identifying old assets to remove..."
            gh release view x86_64 --json assets -q '.assets[].name' > current_assets.txt || touch current_assets.txt
            
            while read -r pkgname; do
              echo ":: Processing cleanup for: $pkgname"
              new_files=$(ls release_dist/${pkgname}-[0-9]*.pkg.tar.zst | xargs -n1 basename)
              
              grep "^${pkgname}-[0-9]" current_assets.txt | while read -r asset; do
                if [[ "$new_files" != *"$asset"* ]]; then
                   echo "   Deleting old version: $asset"
                   gh release delete-asset x86_64 "$asset" -y
                fi
              done
            done < release_dist/updated_pkgnames.txt
          fi

      - name: Upload Packages
        if: steps.check.outputs.BUILD_REQUIRED == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if ! gh release view x86_64 >/dev/null 2>&1; then
            gh release create x86_64 --title "Arch Linux Repository (x86_64)" --notes "Arch Linux AUR Repository"
          fi
          
          echo "Uploading assets..."
          gh release upload x86_64 release_dist/* --clobber
