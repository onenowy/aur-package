# Maintainer: Gustavo Alvarez <sl1pkn07@gmail.com>
# Contributor: MatMoul <matmoul at the google email domain which is .com>

pkgname=qt-sudo
pkgver=2.4.0
pkgrel=4
pkgdesc='A clone of LXQt sudo tool, without LXQt libs'
arch=('x86_64')
url='https://github.com/aarnt/qt-sudo.git'
license=('LGPL2.1')
depends=(
  'qt6-base'
  'sudo'
)

makedepends=(
  'git'
  'qt6-tools'
)
source=("git+https://github.com/aarnt/qt-sudo.git#tag=v${pkgver}")
#source=("git+https://github.com/aarnt/qt-sudo.git")
sha256sums=('e8adc845fa68d17c3ed666f78397b7e11840df2ea6ee234ff76fee3dc306cf')

build() {
  cd qt-sudo
  echo "Starting build..."
  qmake6 \
    PREFIX="${pkgdir}/usr" \
    QMAKE_CFLAGS="${CFLAGS}" \
    QMAKE_CXXFLAGS="${CXXFLAGS}" \
    QMAKE_LFLAGS="${LDFLAGS}"

  make
}

package() {
  cd qt-sudo
  make install
  install -Dm644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
